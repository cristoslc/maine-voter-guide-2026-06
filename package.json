{
  "name": "maine-voter-guide-2026-06",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "preview": "npx @11ty/eleventy --serve --pathprefix=preview",
    "lint": "node -c _data/races.js && npx @11ty/eleventy --dryrun",
    "lint:js": "node -c _data/races.js",
    "test:links": "npx broken-link-checker _site --ordered --recursive"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
